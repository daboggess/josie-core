// Copy to config.js through scripts/Initialize-JosiePrayerBridge.ps1.
// Never commit the generated token.
globalThis.JOSIE_PRAYER_CONFIG = Object.freeze({
  bridgeUrl: "http://127.0.0.1:8788",
  token: "LOCAL_TOKEN_GOES_HERE"
});
